super + m
	firefox

super + Return
	urxvt -fn 'xft:xos4 Terminus:pixelsize=20'

super + @space
	dmenu_run

super + Escape
	pkill -USR1 -x sxhkd

super + alt + Escape
	bspc quit

super + {_,shift + }w
	bspc node -{c,k}

